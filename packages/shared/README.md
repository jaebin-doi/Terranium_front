# packages/shared

Reserved for shared domain definitions.

Expected contents include API contracts, validation schemas, constants, shared TypeScript types, and generated clients once the frontend and backend are implemented.
