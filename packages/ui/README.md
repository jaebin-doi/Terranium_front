# packages/ui

Reserved for shared UI components and the Terranium design system.

This package should eventually hold reusable React components, layout primitives, map/viewer controls, and shared visual tokens used by `apps/web`.
