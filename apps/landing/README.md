# apps/landing

Current production static landing site.

This app is plain HTML/CSS/JS and can be served directly:

```powershell
npx serve apps/landing
```

Static hosting should use this directory as the document root.
