# infra/docker

Reserved for Docker and Docker Compose configuration.

This should eventually define local and pilot environment services such as the web app, API server, PostgreSQL/PostGIS, Redis, object storage, workers, and reverse proxy.
