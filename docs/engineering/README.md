# Engineering Documentation

This folder contains development workflow, local execution, branch strategy, and engineering process documents.

Current documents:

- `DEVELOPMENT.md`: local preview, deployment baseline, and Git branch strategy.

Rules:

- Keep Git and workflow rules here.
- Keep technical implementation plans in `docs/mvp/`.
- Keep user-facing Korean progress reports in `docs/reports/`.
